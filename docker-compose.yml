services:
  localstack:
    # LocalStack Pro required for RDS support.
    # On Windows/Docker Desktop the daemon socket is exposed as a named pipe;
    # we mount BOTH so LocalStack can spawn the postgres child container.
    image: localstack/localstack-pro:latest
    container_name: localstack_project
    ports:
      - "4566:4566"
      - "4510-4559:4510-4559"
    environment:
      - LOCALSTACK_AUTH_TOKEN=${LOCALSTACK_AUTH_TOKEN}
      # Pro: emulated DMS API (see LOCAL_DEV_GUIDE.md §6 and setup_dms_cdc.ps1)
      - ENABLE_DMS=1
      - DEBUG=1
      - DOCKER_HOST=unix:///var/run/docker.sock
      # Same user-defined network as compose (see LocalStack MAIN_DOCKER_NETWORK) so Lambdas resolve the stack.
      - MAIN_DOCKER_NETWORK=bpn-network
    volumes:
      # Docker socket — lets LocalStack spawn postgres as a sibling container
      - "//var/run/docker.sock:/var/run/docker.sock"
      - "localstack_data:/var/lib/localstack"
    networks:
      - bpn-network

networks:
  bpn-network:
    name: bpn-network
    external: false

volumes:
  localstack_data:
    name: localstack_data
